name: ble_chess_example
description: BLE Chess example.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  ble_backend_factory: ^1.3.0
  ble_backend_screens: ^1.1.0
  fluttertoast: ^8.2.12
  flutter_chess_board:
    git:
      url: https://github.com/dkwach/flutter_chess_board
  ble_chess_peripheral_driver:
    path: ../../

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true